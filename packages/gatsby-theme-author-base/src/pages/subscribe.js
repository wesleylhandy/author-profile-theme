import React from "react"
import Layout from "../components/layout"

const SubscribePage = ({location}) => (
    <Layout heading="Subscribe Page" location={location}></Layout>
)

export default SubscribePage