import React from "react"
import Layout from "../components/layout"

const ContactPage = ({location}) => (
    <Layout heading="Contact Page" location={location}></Layout>
)

export default ContactPage